interface:
  display_name: "Maintain Command Center AI"
  short_description: "Synchronize every axis after a package change"
  default_prompt: "Use $maintain-command-center-ai to decide and update every compatibility axis this change to Command Center AI touches, then verify it."
