{
  "actions": [
    {
      "id": "archive",
      "label": "Archive code repositories",
      "endpoint": "/code-repositories/actions/archive/",
      "preflight_endpoint": "/code-repositories/actions/archive/preflight/",
      "method": "POST",
      "tone": "danger",
      "selection_modes": ["explicit", "all_matching"],
      "confirmation": {
        "title": "Archive code repositories",
        "word": "ARCHIVE",
        "button_label": "Archive",
        "warning": "Archived projects stop running."
      },
      "options": [
        {
          "key": "delete_outputs",
          "type": "boolean",
          "default": false,
          "label": "Delete outputs",
          "description": "Also delete generated outputs."
        }
      ]
    }
  ]
}
