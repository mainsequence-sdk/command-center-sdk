{
  "actions": [
    {
      "id": "archive",
      "label": "Archive projects",
      "endpoint": "/projects/actions/archive/",
      "preflight_endpoint": "/projects/actions/archive/preflight/",
      "method": "POST",
      "tone": "danger",
      "selection_modes": ["explicit", "all_matching"],
      "confirmation": {
        "title": "Archive projects",
        "word": "ARCHIVE",
        "button_label": "Archive",
        "warning": "Archived projects stop running."
      },
      "options": [
        {
          "key": "delete_outputs",
          "type": "boolean",
          "default": false,
          "label": "Delete outputs",
          "description": "Also delete generated outputs."
        }
      ]
    }
  ]
}
