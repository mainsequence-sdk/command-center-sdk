{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:mainsequence:command-center-sdk:schema:bulk-action-preflight:v1",
  "title": "Command Center bulk-action preflight response v1",
  "description": "Base backend response indicating whether a discovered bulk action may execute.",
  "type": "object",
  "required": ["allowed"],
  "additionalProperties": true,
  "properties": {
    "allowed": {
      "type": "boolean"
    },
    "detail": {
      "type": "string"
    },
    "matched_count": {
      "type": "integer",
      "minimum": 0
    },
    "blockers": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "warnings": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "$comment": "Domain-specific preflight extensions are allowed. The SDK base parser consumes allowed, detail, matched_count, blockers, and warnings and preserves the full raw object."
}
