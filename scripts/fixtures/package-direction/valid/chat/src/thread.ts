// The allowed direction: the chat imports the SDK. The check never reads the chat's side.
import { Button } from "@dev-mainsequence/command-center-sdk/controls";

export const ThreadAction = Button;
